function countFrequency(lottoNumbers) {
  // 여기에 구현하세요
  // Map을 사용하여 각 번호의 출현 횟수 반환
  // 예: Map { 1 => 3, 2 => 1, ... }
}

function findMostFrequent(lottoNumbers) {
  // 여기에 구현하세요
  // 가장 많이 나온 번호들을 배열로 반환 (동점이면 모두 포함)
}

function findDuplicates(arr) {
  // 여기에 구현하세요
  // Set을 사용하여 중복된 요소만 배열로 반환
}

module.exports = {
  countFrequency,
  findMostFrequent,
  findDuplicates
};