// 미로: 0은 통행 가능, 1은 벽
// 시작점 (0,0), 도착점 (n-1, m-1)

function findPathDFS(maze) {
  // 여기에 구현하세요
  // DFS로 경로를 찾고, 경로가 있으면 true, 없으면 false 반환
}

function findPathBFS(maze) {
  // 여기에 구현하세요
  // BFS로 최단 경로 길이 반환, 경로가 없으면 -1 반환
}

module.exports = {
  findPathDFS,
  findPathBFS
};